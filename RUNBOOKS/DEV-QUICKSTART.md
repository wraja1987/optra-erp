# Dev Quickstart

## Prereqs
- pnpm >= 9
- Node >= 18

## Install
- pnpm install

## Run gates
- pnpm -w lint && pnpm -w typecheck && pnpm -w test && pnpm -w cov && pnpm -w a11y && pnpm -w gate:phase0
